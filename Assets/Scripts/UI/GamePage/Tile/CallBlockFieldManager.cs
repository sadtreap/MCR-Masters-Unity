using UnityEngine;
using MCRGame.Common;
public class CallBlockFieldManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField]
    private GameObject[] mainField = new GameObject[4];

    
}
